overwrite: true
schema: "./src/schema.ts"
require:
  - ts-node/register
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
    config:
      mappers:
        Transaction: "TransactionDbObject"
